#!/bin/sh

# Set environment variables
# export HUBOT_SLACK_TOKEN=xoxb-YOUR-TOKEN-HERE
# ./bin/setenv.sh

set -e

npm install
export PATH="node_modules/.bin:node_modules/hubot/node_modules/.bin:$PATH"

exec node_modules/.bin/hubot --name "first-hubot" "$@"
